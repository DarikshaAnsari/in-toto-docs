{
  "name": "in-toto-docs",
  "scripts": {
    "build": "cd themes/docsy && git submodule update -f --init && cd ../.. && hugo",
    "serve": "hugo serve"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.1",
    "postcss-cli": "^9.1.0"
  }
}